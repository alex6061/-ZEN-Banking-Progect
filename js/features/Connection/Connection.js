export const Select = () => {

  const {} = data; 

  return `
    <select class="select input" placeholder="Connection">
      <option value="${value}">${content}</option>
      <option value="${value}">${content}</option>
      <option value="${value}">${content}</option>
    </select>
  `;
};
